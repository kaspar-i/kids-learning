# Loop 5 Review — Practicality & Completeness (Convergence Verification)

**Reviewer focus:** verification that the loop-4 fixes hold; career reachability from tier 0; acyclic and sensible prerequisites; JSON-vs-prose consistency; usable exercise guidance; difficulty-ramp realism and cadence arithmetic.
**Documents reviewed in full:** `docs/methodology/combined-framework.md` (v1.4), `curriculum/learning-path.md` (v1.4). Loop-4 review files used as the fix checklist; research reports spot-checked where a loop-4 fix added a citation (R3 §5 multiplication-timing row, verified at R3 line 242: "Concept-before-drill (SG) is the defensible order... All converge on fluency by 9").
**Method:** the JSON block was extracted and validated mechanically — parse, unique ids, full reference resolution (`prereqs`, `careers`, `quest`, `capstone_prereqs`, `common_trunk`), toposort acyclicity, tier monotonicity, edge census, raw cross-domain census, bridge derivation with the framework §6 exclusion lists, fan-in census, per-tier node counts, root census, ageBand/tier consistency, trunk ancestor-closure, per-career capstone closures and computed highlights (closure ∪ trunk) with English-strand membership and TR6 root/domain minima, scoped boss-ancestry scan, keystone census with composition breakdown, strict tier-3 chain-end census, `check`-field validity, quest single-shot check, tier-0–3 leaf census, tier-5 consumption check, longest intra-tier chains, longest tier-0–3 spine, and DIG-in-closure census. Additionally the md JSON block was parsed and compared object-for-object against `curriculum/learning-path.json` and the `DATA` constant embedded in `curriculum/skill-tree.html`.
**Date:** 2026-08-30.

**Verdict up front: CONVERGED, no blocking issues, no new findings.** Every loop-4 finding — the practicality six and the pedagogy F1–F8 — is verifiably fixed in v1.4, every fix is correct as applied (none introduced a new inconsistency), every structural number in both documents recomputes exactly, and the three copies of the graph data (md block, `learning-path.json`, viewer `DATA`) are content-identical with all node ids stable. Nothing remains that would mislead a user, contradict the research, break the data contract, or falsify an arithmetic claim.

---

## 1. Verification — every loop-4 finding is FIXED and holding

**Loop-4 practicality findings 1–6:**

1. **Doctor SEL enrichment label — FIXED.** §4's doctor line now reads "enrichment: the SEL chain for empathy — it reaches the doctor highlight via the quest door (`quest-doctor` requires `sel-conflict-resolution`) and the trunk union, not the capstone closure." Machine-verified: doctor's capstone closure (44 nodes) contains zero SEL nodes; `quest-doctor.prereqs` contains `sel-conflict-resolution`; the trunk carries the SEL core. Every clause of the new sentence is true against the data. All other §4 enrichment labels re-checked in-closure/out-of-closure: entrepreneur's `com-present` (out — labeled), finance's and software's sudoku lines (out — labeled), engineer's `cre-iterate-v2` (in — unlabeled, correct), doctor's motor and literacy chains (in — unlabeled, correct).
2. **`num-fractions` keystone — FIXED.** Tagged `keystone: true`; GP13's leg-capping example now names "numeracy's problems-and-data and fractions legs"; learning-path §6 preamble says "numeracy caps both its problems-and-data and fractions legs, as literacy caps three — 23 keystones in v1.4." Machine census: **exactly 23 keystones** (2 bosses + 8 quests + 13 branch-leg caps), and a strict census of tier-3 chain-ends (all consumers tier ≥4) now leaves exactly one untagged node — `dig-typing`, which GP13's stated micro-node exclusion covers and whose card says "Micro-node." §7's "keystone set matching GP13's definition" validation claim is now literally reproducible. No stale "22" remains in either document.
3. **"No third pass" collision — FIXED.** §5.1 now counts in *check events*: "two verification stages — the §5.2 mastery check (one check event, itself run in two sittings ≥3 days apart per Archetype A), then the strip-borne retest; no further check event exists, **and neither stage may be collapsed to fewer sittings than its own spec names**." The collapse-risk reading loop 4 flagged is closed explicitly.
4. **Boss 2 interval deviation — FIXED.** `num-times-tables.mastery_check` now carries the in-place flag: "passed twice at least a week apart — a flagged, deliberate widening of Archetype A's ≥3-day default (which num-addsub-20 follows): a full-korrutustabel automaticity claim earns the longer verification gap."
5. **`mot-handwriting-auto` template slot — FIXED.** The weekly copy passage now names its carrier: "riding the booklet sessions' form-drawing/warm-up lane (framework §3 template — the same lane letter formation rides at Band 2, never a separate slot)" — consistent with §3 guard-rail (a), which now names "handwriting's weekly copy passage at Bands 3–4" as a warm-up-riding exempt strand. Sweep re-run: every desk-work cadence line in the learning path names a §3 slot (fluency-maintenance sessions for tables, booklet-session openers for typing, warm-up lane for handwriting/letter formation, strips for retrieval); remaining cadence-bearing lines are life-side and exempt.
6. **`spa-scale-drawings` hidden arithmetic — FIXED.** The card carries the assumes-line in the quest idiom: "Scales stay friendly... 1 cell = one floor tile or one big step; numeric scale CONVERSION assumes the num-tables-1 frontier, and if that node is still open the parent co-pilots the arithmetic" — explicitly a soft line, never an edge, with the scoped-boss-rule reasoning stated on the card.

**Loop-4 pedagogy F1–F8** (the ones not duplicated above):

- **F1 (cap scoping) — FIXED, both occurrences.** §3 guard-rail (a): "at most two training-ground nodes **whose dose occupies a template session slot**... a strand whose minutes ride the booklet sessions' warm-up pages... or that is school-evidenced (`lit-reading-fluency`) does not count against the cap, so the band templates' three named concurrent strands are the cap working as designed, never an exception to it." §5.6 repeats the scoped form ("two *slot-occupying* training-ground nodes... §3 guard-rail (a)"). Recomputed: Band 2 = phonics-1 + a staged num node slot-occupying (2), letter formation exempt; Band 3 = phonics-2 + a number-fact node (2), handwriting and `lit-reading-fluency` exempt. The cap now states a true constraint the templates satisfy.
- **F2 (Band-3 times tables) — FIXED.** The cell reads "number-fact retrieval within 20/100 — first tables late in the band only as mastery allows (table *retrieval* proper is tier-3/Band-4 content: concept before drill, R3 §5 multiplication-timing row)" — now consistent with the graph's tier-3 `num-tables-1`, the Band-3 math-frontier row, and the tier-2 walkthrough; the R3 citation verified.
- **F3 (quest-scientist door) — FIXED.** §4 now says "reachable from tier-2 prerequisites (`quest-scientist` alone additionally requires the tier-3 `sci-fair-test`, by design...)". Verified against all eight quests' prereq tiers: seven are all-tier-≤2; only quest-scientist carries a tier-3 prereq.
- **F5 (`sci-observe-senses`) — FIXED.** Now `check: "mixed"` with sub-check (c) named single-shot at completion, matching the `num-money-time` precedent exactly.
- **F7 (thin booklets vs B18) — FIXED.** §4.4 note 4: "the entry pair splits one standard booklet's page budget between 1a and 1b — B18's 20–28 pages / 8–10 working spreads apply to the *pair as a whole*, not to each thin half, so 'thin' deviates from no size rule (and Band-1 spacing is unaffected either way, since Band 1 keys gap labels to elapsed time, B18)."

---

## 2. Machine-verified structural state (v1.4)

- **Parses as JSON; 97 nodes** (18/18/17/25/10/9 — §1 table, §7 note, and invariant 4 all agree); 8 careers; 26 trunk ids; all references resolve; ids unique; acyclic; every edge same-tier-or-earlier; **124 edges**; **31 raw cross-domain (exactly "31 of 124", 25.0%)**.
- **Bridge derivation reproduces:** applying framework §6's exclusion lists (EF out-edges, 7 named corequisites — all 7 present as edges — quest fan-in, tier-4/5 fan-in) leaves exactly `lit-letter-knowledge → mot-letter-formation` and `mot-letter-formation → log-sudoku-symbols`. Invariant 9 exact. (Note for future validators: §6(a)'s "exactly two EF out-edges in tiers 0–3" counts edges into *branch* nodes — the two EF→quest edges fall under carve-out (c), quests sitting above the domains; counted that way the claim is exact, and the final bridge list is the same under either order of exclusion.)
- **14 roots, all tier 0**; no non-tier-0 node without prereqs; fan-in ≤2 everywhere except `t5-research-project` = 3; ageBand/tier consistent on all 97 nodes; `check` values all valid; all 8 quests `single-shot`.
- **Trunk ancestor-closed** (zero violations); **all eight careers' computed highlights contain the full English strand** (`lit-en-phonics-1/2`, `lit-en-oral-3`, `lit-en-reading`); TR6 minimum found: 7 tier-0 roots across 5 domains (manager, finance) — invariant 8 holds with margin.
- **Scoped boss rule:** zero tier-0–3 non-numeracy branch nodes carry either boss in hard ancestry.
- **Keystones = 23** (2 + 8 + 13), matching both documents' stated composition; strict chain-end census leaves only the documented micro-node untagged.
- **Tier-0–3 leaves:** exactly `mot-gross-body`, `log-sudoku-master`, and the 8 quests — all documented; every tier-5 node is consumed by some career's `capstone_prereqs`.
- **Chains:** longest intra-tier chain 3 (invariant 5's budget ≤6); longest tier-0–3 spine is exactly the 9-node numeracy chain invariant 5 names, verified node-by-node as the global maximum.
- **"Seven of eight careers reach 18 with zero DIG in their closure"** — machine-exact (only software-engineer's capstone closure contains DIG nodes).
- **Data contract:** the md JSON block, `curriculum/learning-path.json`, and the `DATA` constant in `curriculum/skill-tree.html` are **content-identical** (parsed and compared object-for-object). v1.4 changed no node ids (only added `keystone` on `num-fractions`, `check` on `sci-observe-senses`, and description/`mastery_check` text), so all poster/viewer references remain stable.
- **Cadence arithmetic re-checked where v1.4 touched it:** §5.6 child-minutes rows still equal template sessions × CR16 dose lengths for all four bands; B18's 8–10 spreads ÷ 2 booklet sessions = 4–5 weeks ≈ one month; strip arithmetic 5–6 × 4 = 20–24 items with ≤8 consumed by ≤2 retest nodes; the scoped cap makes §5.6's feasibility argument true rather than charitably-true.

## 3. Checked and found clean (near-misses deliberately not reported)

Three seams were examined and judged non-issues under the convergence criteria, recorded here so later editors don't relitigate them: (a) the §6 EF "exactly two" count, exact under the quest carve-out reading both prior loops used (see §2 above); (b) `num-tables-1`/`num-times-tables` saying table items ride "the daily retrieval strips" — the doctrine's own idiom (§3: "'Daily' prescriptions do not exist outside the ~2-minute strips"), with the boss card itself adding "nothing lands on non-session days"; (c) `ef-plan-check`/`ef-multistep-projects` defaulting to `check: "A"` — a design choice that passed loops 3 and 4, both nodes having table-runnable performance components.

## 4. Convergence judgment

**Converged, cleanly.** All twelve loop-4 findings are fixed, correctly and without collateral damage; the graph, both documents' arithmetic, and the three-way data contract are exactly true under mechanical verification; no finding meets the reporting bar. The tier-0 poster, entry booklets 1a/1b, and the strip/retest tooling can build on v1.4 as-is. Recommend ending the review loop here.
