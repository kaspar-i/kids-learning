# Review Loop 5 — Pedagogical Soundness (convergence pass)

**Reviewer focus:** verify the loop-4 fixes hold; report only issues that would mislead a parent or teacher, contradict the research evidence, break the JSON data contract or graph invariants, or make textual claims provably false.
**Documents reviewed in full:** `docs/methodology/combined-framework.md` (v1.4), `curriculum/learning-path.md` (v1.4). Research reports re-consulted on the two citations the loop-4 rewordings lean on (R4 A6.7 / line 153 two-pass consolidation; R3 §5 multiplication-timing row + §5 booklet-pipeline line "tables via arrays and skip-counting tracks (8)").
**Method:** the JSON block was extracted and re-validated mechanically — parse; unique ids; reference resolution (`prereqs`, `careers`, `quest`, `capstone_prereqs`, `common_trunk`); acyclicity; tier monotonicity; node census (97 = 18/18/17/25/10/9); edge census (124); root census (14, all tier 0); fan-in (≤2 everywhere except `t5-research-project` = 3); ageBand/tier consistency (0 mismatches); raw cross-domain census (exactly 31 of 124 = 25.0%); bridge derivation with the framework §6 exclusion lists applied edge-by-edge (exactly the two named bridges reproduce); EF out-edge census (exactly the two named, in tiers 0–3, branch-level); trunk ancestor-closure (26 ids, zero violations); per-career highlight computation (closure ∪ trunk — all eight contain the full English strand; ≥9 tier-0 roots across ≥5 domains each); scoped boss rule (zero tier-0–3 non-numeracy branch violations); keystone census (exactly 23 = 2 bosses + 8 quests + 13 branch-leg caps; the only untagged tier-3 chain-end is `dig-typing`, covered by GP13's micro-node exclusion); `check`-field validity (all values legal; all 8 quests `single-shot`); intra-tier chain lengths (max 3); the 9-node tier-0–3 numeracy spine (verified node-by-node as the longest); leaf scan (only the documented leaves); inline boss checks present. Additionally the md JSON block, `curriculum/learning-path.json`, and the `DATA` constant embedded in `curriculum/skill-tree.html` were parsed and compared — **all three are identical in content**, and v1.4 changed no node ids, so the viewer contract is intact.
**Date:** 2026-08-30.

---

## 0. Verification — every loop-4 finding is fixed and holding

All fourteen loop-4 findings (8 pedagogy, 6 practicality, with overlaps) verified in the actual v1.4 text and data:

- **F1 (the one major) — FIXED.** The ≤2-concurrent-training-grounds cap is now scoped to *slot-occupying* strands in both places it appears (§3 subordination rule, §5.6 pruned-by-rule note): a strand riding the booklet sessions' warm-up pages (letter formation at Band 2; handwriting's weekly copy passage at Bands 3–4) or evidenced by school work (`lit-reading-fluency`) does not count. Re-checked against the band templates: Band 2's slot-occupying training grounds are exactly phonics-1 + one staged numeracy node (= 2, letter formation exempt); Band 3's are phonics-2 + one numeracy node (= 2, reading-fluency and handwriting exempt). The cap and the templates now agree — the stated reason in §5.6 is true, not just its conclusion.
- **F2 — FIXED.** The Band-3 direct-instruction cell now reads "number-fact retrieval within 20/100 — first tables late in the band only as mastery allows (table *retrieval* proper is tier-3/Band-4 content: concept before drill, R3 §5 multiplication-timing row)" — consistent with the graph's tier-3 `num-tables-1`, the Band-3 math-frontier row, the tier-2 walkthrough, and R3 §5 (verified: "All converge on fluency by 9. Concept-before-drill (SG) is the defensible order"; R3's own booklet pipeline places tables at 8).
- **F3 — FIXED.** §4's quest-door sentence now carries the clause "`quest-scientist` alone additionally requires the tier-3 `sci-fair-test`, by design." Verified: all seven other quests' prereqs are tier ≤2.
- **F4 / practicality 3 — FIXED.** §5.1 now counts in *check events*: "two verification stages — the §5.2 mastery check (one check event, itself run in two sittings ≥3 days apart per Archetype A), then the strip-borne retest; no further check event exists, and neither stage may be collapsed to fewer sittings than its own spec names." The collapsibility loophole is closed; "no third pass exists" is gone from the live documents.
- **F5 — FIXED.** `sci-observe-senses` carries `check: "mixed"` with each sub-check's archetype named — (a) A, (b) A, (c) the week-long weather strip, single-shot at completion — explicitly the same treatment as `num-money-time`'s savings jar.
- **F6 / practicality 4 (the loop-3 m11 carry-over) — FIXED.** `num-times-tables`' inline check now flags the deviation in place: "passed twice at least a week apart — a flagged, deliberate widening of Archetype A's ≥3-day default (which num-addsub-20 follows): a full-korrutustabel automaticity claim earns the longer verification gap."
- **F7 — FIXED.** §4.4 note 4 now rules B18 sizing for the entry pair: 1a/1b split one standard booklet's page budget, B18's 20–28 pages / 8–10 spreads apply to the pair as a whole, and Band-1 spacing is unaffected (elapsed-time keying). "Thin" no longer deviates from any rule.
- **F8 / practicality 2 — FIXED.** `num-fractions` is tagged `keystone: true` with an on-card rationale; GP13's leg-capping example now names numeracy's problems-and-data and fractions legs; both documents say 23 keystones. Machine census: exactly 23 = 2 bosses + 8 quests + 13 branch-leg caps, and the only untagged tier-3 chain-end is `dig-typing` (micro-node, covered by GP13's stated exclusion) — the keystone set is now reproducible from the definition.
- **Practicality 1 — FIXED.** The doctor spine's SEL parenthetical is labeled enrichment with the honest route stated (quest door + trunk union, not the capstone closure). Machine-verified: doctor's capstone closure indeed contains zero SEL nodes, while `quest-doctor` requires `sel-conflict-resolution`.
- **Practicality 5 — FIXED.** `mot-handwriting-auto`'s weekly copy passage now names its template slot (the booklet sessions' form-drawing/warm-up lane, "the same lane letter formation rides at Band 2, never a separate slot"), and §3 guard-rail (a) names it in the exempt list. Sweep: no desk-work cadence line is slotless.
- **Practicality 6 — FIXED.** `spa-scale-drawings` carries the friendly-scales assumes-line in the quest-card idiom (1 cell = one floor tile / one big step; numeric conversion assumes the `num-tables-1` frontier, parent co-pilots otherwise), explicitly as an assumes-line, never an edge — the scoped boss rule stays intact (machine-verified: zero violations).

**Changelog honesty:** every claim in both v1.4 changelog paragraphs was verified true against the text and data. **Citation spot-checks:** R4 line 153 ("mastery check passed twice, ≥2 weeks apart") and R3 §5's multiplication-timing row say what the revised text says they say.

---

## 1. Findings

**None at critical or major severity, and no genuine minors found.** The candidates examined and rejected, for the record:

- `ef-multistep-projects` defaults to `check: "A"` though it is project-shaped — examined and left: unlike loop-4's F5 there is no structurally identical precedent that earned a different tag, a small re-run project is a workable A-style re-pass, the booklet pipeline authors the actual check, and the node has survived four review sweeps including two mechanical `check`-field audits. Reopening it would be churn, not convergence.
- "Daily retrieval strips" phrasing on the two table-node cards — consistent with the framework's own usage ("'Daily' prescriptions do not exist outside the ~2-minute strips"), and the `num-times-tables` card itself adds "nothing lands on non-session days (which by rule carry no system work)."
- §5.1's parenthetical states Archetype A's ≥3-day default while boss 2 uses a week — the deviation is flagged at the point of deviation per the house convention, which is exactly what loop 4 asked for.
- `sci-fair-test` is keystone-tagged though its consumers include tier-3 `quest-scientist` — consistent under the doctrine's own reading (quests sit above the domains, so it is the SCI branch's tier-3 chain-end), and the tag is normative in any case.

---

## 2. Verdict

**Converged, cleanly.** Every loop-4 finding — the one major and all thirteen minors across both reviews — is verifiably fixed in v1.4; every structural, arithmetic and display claim recomputes exactly (97/124/31/2/26/23/14 and the per-career highlight guarantees); the md block, `learning-path.json` and the viewer's embedded `DATA` are identical with stable node ids; the revised prose contradicts neither its own rulings nor the research reports. Nothing blocking remains and no new genuine minors were found. This version is fit for use: ship the tier-0 poster and entry booklets 1a/1b against it, and stop the fine-tuning loop per the convergence rule (0 critical / 0 major).
